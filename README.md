# APIs in Action – Video API

## Overview
A Node.js + Express + MongoDB REST API built and tested entirely on Termux (mobile).  
This API allows adding and retrieving videos using simple POST and GET endpoints.

---

## 🧩 Setup Instructions

1. Clone this repository:# APIs in Action – Video API

## Overview
Simple Node.js + Express + MongoDB REST API built and tested on Termux.

## Setup
1. Run `npm install`
2. Create a `.env` file:
